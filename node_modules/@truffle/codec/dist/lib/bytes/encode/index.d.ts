import type * as Format from "../../format";
/**
 * Encodes without padding, length, etc!
 *
 * @Category Encoding (low-level)
 */
export declare function encodeBytes(input: Format.Values.BytesDynamicValue | Format.Values.StringValue): Uint8Array;
